<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Foreground service (stream receiver) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />

    <!-- Camera (required to register as a virtual camera provider) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Wake lock – prevent CPU sleep during streaming -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Camera feature declaration (we provide virtual camera) -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.VirtualCam">

        <!-- Main Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Stream Receiver Foreground Service -->
        <service
            android:name=".service.StreamService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection"
            android:stopWithTask="false" />

        <!-- Virtual Camera Provider Service (API 33+) -->
        <service
            android:name=".camera.VirtualCameraService"
            android:exported="true"
            android:permission="android.permission.CAMERA">
            <intent-filter>
                <action android:name="android.hardware.camera2.CameraExtensionsService" />
            </intent-filter>
        </service>

    </application>

</manifest>
